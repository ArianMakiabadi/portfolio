export type StartMenuAppItem = {
  id: string;
  title: string;
  subtitle: string;
  icon: string;
};

export type StartMenuLinkItem = {
  id: string;
  label: string;
  icon: string;
  href: string;
};
